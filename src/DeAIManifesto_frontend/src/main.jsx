import React from 'react';
import ReactDOM from 'react-dom/client';
import AIManifestoPage from './AIManifestoPage';
import "./app.css"

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AIManifestoPage />
  </React.StrictMode>,
);
